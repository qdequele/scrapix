{
  "name": "scrapix-contracts",
  "private": true,
  "version": "0.1.0",
  "description": "Backend-agnostic contract tests for the Scrapix SaaS API (SCR-85). Run against the Rust API today, the Rails app as routes migrate.",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
